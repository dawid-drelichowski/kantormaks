{
  "name": "kantormaks-website",
  "version": "1.0.0",
  "description": "Kantormaks website based on Fastify",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js & npm run assets:dev",
    "assets:build": "glob-exec --foreach --parallel \"assets/css/{home,error,admin}.css\" -- \"lightningcss --bundle --minify --sourcemap {{file}} -o public/css/{{file}}\" && swc assets/js -d public --strip-leading-paths",
    "assets:dev": "chokidar 'assets/css/*.css' --initial -c 'lightningcss {path} --output-dir public/css' & swc assets/js -d public --strip-leading-paths --no-swcrc --watch",
    "lint": "eslint .",
    "test": "playwright install chromium && playwright test"
  },
  "author": "Dawid Drelichowski",
  "license": "ISC",
  "type": "module",
  "dependencies": {
    "@fastify/basic-auth": "^6.0.1",
    "@fastify/formbody": "^8.0.1",
    "@fastify/static": "^9.1.3",
    "@fastify/view": "^12.0.0",
    "fastify": "^5.8.5",
    "handlebars": "^4.7.8",
    "qs": "^6.15.2"
  },
  "imports": {
    "#config": "./config.js"
  },
  "devDependencies": {
    "@playwright/test": "^1.61.0",
    "@swc/cli": "^0.8.1",
    "@types/node": "^25.9.2",
    "glob-exec": "^0.1.1",
    "lightningcss-cli": "^1.27.0"
  }
}
